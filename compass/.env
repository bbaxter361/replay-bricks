# Compass Frontend Environment Variables
# These are embedded at build time (Vite VITE_ prefix)
# In production, set VITE_SPRING_API_KEY as a Netlify build environment variable
VITE_SPRING_API_KEY=spring-vicki-2026
